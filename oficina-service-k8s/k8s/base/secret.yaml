# IMPORTANTE: Este arquivo é apenas para referência.
# Em produção, NUNCA commite secrets no Git!
# Use ferramentas como Sealed Secrets, Vault ou External Secrets Operator.
#
# Para criar o secret corretamente, use:
# kubectl create secret generic oficina-secrets -n oficina \
#   --from-literal=DB_USER=oficina_user \
#   --from-literal=DB_PASS=oficina_pass \
#   --from-literal=JWT_SECRET=seu-jwt-secret-aqui \
#   --from-literal=MAIL_USER=seu-email@gmail.com \
#   --from-literal=MAIL_PASS=sua-senha-email

apiVersion: v1
kind: Secret
metadata:
  name: oficina-secrets
  namespace: oficina
type: Opaque
stringData:
  # Use stringData em vez de data para valores não codificados
  # O Kubernetes fará a codificação Base64 automaticamente
  DB_USER: oficina_user
  DB_PASS: oficina_pass
  JWT_SECRET: a29mYXNtdGVuNm11aXRvc2VndXJhZXNlY3JldGFkb3NlamNyZXRvZGFvZmljaW5h
  MAIL_USER: mailtrap_user
  MAIL_PASS: mailtrap_pass
